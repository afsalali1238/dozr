Dispatch the `ux-wireframe-architect` agent (via Task tool) to wireframe: $ARGUMENTS

If no page/flow is named, ask which page from ROADMAP.md Phase 1's list
(home, equipment/freight browse, booking form, rate cards, tracking page,
quote approval) before proceeding.

Remind the agent to check `LOGISTICS/05_Brand_Design/Dozr_Wireframes.html`
and `Dozr_Marketplace_Prototype.html` first — audit/iterate on what already
exists rather than starting blank.
