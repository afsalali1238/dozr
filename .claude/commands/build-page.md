Dispatch the `frontend-builder` agent (via Task tool) to build: $ARGUMENTS

Before dispatching, confirm this page has an approved wireframe (ux-wireframe-architect)
and copy (copywriter) already in hand — if either is missing, do that step first
instead of letting frontend-builder invent layout or copy.

After the build, run `/brand-check` and dispatch `qa-accessibility-reviewer`
before considering the page done.
