#!/bin/bash
# SessionStart hook — echoes into context on every session start.
# Keep this fast and low-noise; it runs every time.

echo "Dozr project — brand source of truth: LOGISTICS/05_Brand_Design/Dozr_Brand_Guidelines.html"
echo "Active build plan: ROADMAP.md (Dozr root) — check current phase and owning agent before starting work"
echo "Pre-code stage: no lint/build/test commands configured yet for the Marketplace rebuild"
echo "LOGISTICS/99_Archive/ is superseded material — do not treat as current context"
